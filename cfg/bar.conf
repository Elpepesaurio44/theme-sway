# Nota: Ya usas waybar vía `exec_always`, así que la barra nativa de sway está desactivada.
# Si quieres volver a swaybar, descomenta este bloque y comenta `exec_always pkill waybar; waybar` en autostart.conf

# bar {
#     position top
#     height 22
#     font pango:JetBrainsMono Nerd Font 9
#     colors {
#         statusline $foreground
#         background $background
#         focused_workspace  $color4 $color4 $background
#         active_workspace   $color8 $color8 $foreground
#         inactive_workspace $background $background $color8
#         urgent_workspace   $color1 $color1 $foreground
#     }
# }
